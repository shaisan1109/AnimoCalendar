import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import android.widget.Button
import android.widget.EditText
import com.google.firebase.firestore.FirebaseFirestore
//import com.mobdeve.s11.mco11.animocalendar.databinding.ActivityCreateTaskBinding

class CreateTaskActivity : AppCompatActivity() {
/*
    private lateinit var binding: ActivityCreateTaskBinding
    private lateinit var firestore: FirebaseFirestore

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ActivityCreateTaskBinding.inflate(layoutInflater)
        setContentView(binding.root)

        // Initialize Firebase Firestore
        firestore = FirebaseFirestore.getInstance()

        val cancelButton: Button = binding.cancelBtn
        val createButton: Button = binding.createBtn
        val taskName: EditText = binding.taskNameEt
        val date: EditText = binding.dateTv
        val hostName: EditText = binding.hostEt
        val description: EditText = binding.descriptionEt

        // Set click listener for cancel button
        cancelButton.setOnClickListener {
            // Handle cancel button click
            // For example:
            finish() // Close the activity
        }

        // Set click listener for create button
        createButton.setOnClickListener {
            // Handle create button click
            // For example:
            val taskName = taskName.text.toString()
            val date = date.text.toString()
            val host = hostName.text.toString()
            val description = description.text.toString()

            // Validate inputs, save data to Firestore, etc.

        }
    } */
}

